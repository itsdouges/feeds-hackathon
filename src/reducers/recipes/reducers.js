'use strict';

import * as types from './actions';

const initialState = {

};

export default function recipe(state = initialState, action = {}) {
    switch (action.type) {

        default:
            return state;
    }
}