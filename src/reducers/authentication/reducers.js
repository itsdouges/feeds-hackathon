'use strict';

import * as types from './actions';

const initialState = {

};

export default function authentication(state = initialState, action = {}) {
    switch (action.type) {

        default:
            return state;
    }
}